import { Injectable } from '@nestjs/common';

@Injectable()
export class WebhookEventsListener {
  // @OnEvent(PAYSTACK_WEBHOOK_RECEIVED, { async: true })
  // handleCreatedEvent(event: WebhookEvents) {
  //   console.log('Webhook Event received');
  //   console.log(event);
  // }
}
